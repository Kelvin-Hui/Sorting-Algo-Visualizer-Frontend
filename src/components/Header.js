import React from "react";

function Header() {
  return (
    <div className="header">
      <span style={{ fontSize: `50px` }}>Sorting Algorithm Visualizer</span>
    </div>
  );
}

export default Header;
